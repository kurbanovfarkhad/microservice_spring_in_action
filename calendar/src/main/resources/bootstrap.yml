server:
  port: 6666

spring:
  application:
    name: calendar-service
  profiles:
    active: default
  cloud:
    config:
      uri: http://localhost:5555

security:
  oauth2:
    resource:
      userInfoUri: http://localhost:8888/auth/user
